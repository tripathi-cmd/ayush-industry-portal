PORT=5000
JWT_SECRET=ayush-ministry-secure-jwt-token-key-2026

# Optional: MongoDB Atlas Connection URI
# If set, the server connects to MongoDB. If not set, it uses the integrated zero-config persistent file store (data.json).
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/ayush_portal?retryWrites=true&w=majority
